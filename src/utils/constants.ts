export const TRACKING_CONFIG = {
  HIGH_ACCURACY: 10,
  MEDIUM_ACCURACY: 100,
  LOW_ACCURACY: 1000,
  ACTIVE_INTERVAL: 15000,
  IDLE_INTERVAL: 120000,
  STATIONARY_INTERVAL: 300000,
  MIN_DISPLACEMENT: 25,
  STATIONARY_RADIUS: 80,
  WALKING_SPEED: 2.0,
  DRIVING_SPEED: 20.0,
  FLYING_SPEED: 55.0,
  MIN_DWELL_TIME: 8 * 60 * 1000,
  SHORT_STOP_TIME: 5 * 60 * 1000,
  BATTERY_SAVER_THRESHOLD: 0.2,
} as const;

export const CURATION_CONFIG = {
  MIN_QUALITY_SCORE: 0.45,
  FEATURED_QUALITY_THRESHOLD: 0.75,
  JUNK_THRESHOLD: 0.7,
  MAX_PHOTOS_PER_STEP: 10,
  FEATURED_PHOTOS_PER_STEP: 3,
  TIME_CLUSTER_WINDOW: 60 * 60 * 1000,
  LOCATION_CLUSTER_RADIUS: 250,
} as const;

export const LOCAL_USER_ID = 'local_user';

export const BOOK_PRICING = {
  hardcover: {'8x10': 7999, '11x14': 9999},
  softcover: {'8x10': 4999, '11x14': 6999},
} as const;

export const BOOK_SPECS = {
  MIN_PAGES: 20,
  MAX_PAGES: 200,
  PAGES_PER_STEP: 1,
} as const;

export const FEATURE_FLAGS = {
  ENABLE_BOOK_ORDERING: true,
  ENABLE_DEMO_SIMULATOR: true,
  DEBUG_MODE: __DEV__,
} as const;
